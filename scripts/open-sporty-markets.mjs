#!/usr/bin/env node
// Disabled — sportybet.ts in source is the source of truth.
// Do not rewrite files during `npm run build` (that was breaking Vercel).
console.log("[open-sporty-markets] skipped — using src/lib/sportybet.ts as committed");
process.exit(0);
